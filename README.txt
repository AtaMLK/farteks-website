FARTEKS COMPLETE REQUESTED UPDATE

This package contains the files changed for Ata's requested Farteks website update.

Base: GitHub main commit 0dea53767da4874fa4aa2a44c7b3e667b8206983

Copy/merge the included files into the root of your E:\old farteks repository.
Do NOT delete other files.

Then run:
  npm install
  npm run build

If the build is clean:
  git add .
  git commit -m "Complete Farteks website requested update"
  git push origin <your-branch>
